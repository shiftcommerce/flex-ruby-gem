version: '2.1'
jobs:
  build:
    docker: 
      - image: circleci/ruby:2.7.0
    steps:
      - checkout
      - run: bundle install
      - run: bundle exec rspec
  build:
    docker: 
      - image: circleci/ruby:2.5.3
    steps:
      - checkout
      - run: bundle install
      - run: bundle exec rspec
